import { View, Text, SafeAreaView, Pressable, TextInput } from 'react-native';

const Search: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Isearch ang iyong paboritong ulam</Text>
      <TextInput />
    </SafeAreaView>
  );
};

export default Search;
